/**
 * Booking Feature — Hooks Barrel
 *
 * Re-exports the existing useBookings hook from the root hooks directory.
 * As the feature matures, additional booking-specific hooks live here.
 *
 * @module src/features/booking/hooks
 */

export { useBookings } from "@/hooks/useBookings";
