/**
 * Booking Feature — Flag Declarations
 *
 * Re-exports only the flags that belong to this feature.
 * The booking team owns these flags; they should not import
 * flags from other features directly.
 *
 * @module src/features/booking/flags
 */

export { newBookingFlowFlag, quickRebookFlag } from "@/shared/flags/flags";
